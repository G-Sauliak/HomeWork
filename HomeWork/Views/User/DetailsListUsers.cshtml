@model IEnumerable<HomeWork.Models.UserInfo>
@using X.PagedList
@using X.PagedList.Mvc

<div class="container body-content ">
    <div class="row">
        <div class="col-md-5">
            <div class="panel panel-primary">
                <div class="panel-body" id="userlist">
                    <ul class="nav list-group">
                        @foreach (var item in Model)
                        {
                            <li class="list-group-item">
                                <div class="checkbox">
                                    <span class="glyphicon glyphicon-user"></span>
                                    <label for="checkbox">@item.FirstName @item.LastName</label>
                                </div>
                                <div class="pull-right action-buttons ">
                                    <a href="#" id ="Details" userID="@item.Id"><span class="glyphicon glyphicon-info-sign"></span></a>
                                </div>
                            </li>
                        }
                    </ul>
                </div>
                <div class="panel-footer">
                    <div class="row">
                        <div class="col-md-6">
                            <h6>
                                Total Count: <span class="label label-info" id="total-count">@ViewBag.CountUsers</span>
                            </h6>
                        </div>
                        <div class="col-md-6">
                            <div class="pagination pagination-sm pull-right">

                                @Html.PagedListPager(Model as IPagedList, page => Url.Action("ListUsers", new { page }), PagedListRenderOptions.MinimalWithPageCountText)
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-xs-6">
            <div id="user_content"></div>
        </div>
    </div>
</div>

